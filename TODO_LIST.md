# TODO List

Short- and mid-term actionable work, ranked by impact. Open items only — completed
items live in `CHANGELOG.md`. Every item cites evidence (code) and origin (report).

| # | Priority | Task                                                                                                                                                                                                                                                                                                                                                        | Effort | Evidence / Origin                                                                                                 |
| - | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------- |
| 1 | High     | Release v0.5.0: `metrics/` sits in `[Unreleased]`. Cut CHANGELOG, then tag only after CI is green on the exact commit (release runbook in AGENTS.md), clean-room `go get github.com/larsartmann/go-etag/metrics@v0.5.0`, proxy/sum verification, GitHub Release as Latest non-prerelease (house precedent). Depends on #2.                                  | M      | `CHANGELOG.md` [Unreleased]; planning `docs/planning/2026-09-22_23-25_*.md` follow-up note                        |
| 2 | Medium   | Decide the Go directive floor for v0.5.0: daemon commit `969d077` (unreviewed) relaxed `go 1.27.1` → `go 1.27`. Builds green (`go-error-family` needs only 1.26), but v0.4.0, the README badge, and all three CI pins say 1.27.1. Either restore the patch pin or accept `1.27` and update README badge + AGENTS.md + CI-pin story before tagging.          | S      | `git show 969d077 -- go.mod`; `README.md:5`; `.github/workflows/ci.yml:24,58,76`; report 2026-09-22_21-36 f#3     |
| 3 | Medium   | Consumer propagation sweep for v0.4.0 (then v0.5.0): in-house consumers still pin pre-0.4 versions. Check each consumer's version surface first (flake.nix inputs, vendorHash, CI pins; DiscordSync's drift-guard test), commit immediately after each gate, `nix build` when go.sum changed, verify workspace repos in BOTH modes (recipe in AGENTS.md).   | M      | Report 2026-09-18_21-00 f#1–f#2 (not started); `docs/status/2026-09-11_02-56_*.md` recipe                         |
| 4 | Medium   | Make the LSP usable: gopls and golangci-lint-ls load with the persisted `GOTOOLCHAIN=local` (go1.26.7) and error on every file ("go.mod requires go >= 1.27"), so in-editor diagnostics are dead. Set `GOTOOLCHAIN=auto` in the Crush/gopls launcher config — not the persisted global go env. Needs owner permission (user config).                        | S      | AGENTS.md Commands note; reports 2026-09-18_20-44 g#3, 2026-09-22_21-36 f#13                                      |
| 5 | Medium   | HEAD-freshening hardening (1/2): spec pin that HEAD-freshened entries never persist the `FromCacheHeader` marker — the 06-18 dedup made `freshenFromHead` route through `persistFreshened`, incidentally stripping the marker; reasoning says harmless, but no test pins it.                                                                                | S      | `client/transport.go` (`persistFreshened`); reports 2026-09-11_06-18 b.1/f.1, 2026-09-22_21-36 c.3                |
| 6 | Medium   | HEAD-freshening hardening (2/2): prove-or-delete `cloneHeader`'s nil branch (can `entry.header` ever be nil? unit test or drop the guard); coverage check on `persistFreshened`/`cloneHeader` (`go test -coverprofile`); benchmark the extra `header.Clone()` per freshening HEAD against a git-reachable pre-dedup baseline (`reports/bench/` discipline). | M      | `client/transport.go:445` (`cloneHeader`); reports 2026-09-11_06-18 f.2–f.4, 2026-09-22_21-36 f#7–f#9             |
| 7 | Low      | Capture the before-state benchmark for the stored-validator change from `07fe65c^` (git worktree, no daemon interference) and file the pair — closes the sequencing miss from 09-18 (before-state recoverable, never measured).                                                                                                                             | S      | `reports/bench/2026-09-18_baseline-typed-code-stored-validator.txt` (after-only); report 2026-09-18_20-44 b.1/f.7 |
