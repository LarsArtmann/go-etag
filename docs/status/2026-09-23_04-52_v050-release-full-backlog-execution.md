# Status Report — v0.5.0 Release + Full TODO_LIST Execution

**Date:** 2026-09-23 04:52 CEST · **Session mode:** execute-and-verify ("get the whole TODO list done")
**Scope:** TODO_LIST items #1–#7 in full, plus the plan's pre-tag gates (M7a bench pair, M8 pre-release-check.sh, M9 GoDoc examples) from `docs/planning/2026-09-23_01-40_pareto-v05-cycle-full-backlog.md`.
**State at close:** go-etag clean at `af2b6d1` (= tag `v0.5.0`); all six consumer repos swept and committed locally (unpushed — owner call).

---

## a. Executive summary

The entire TODO_LIST was executed end-to-end: the go-floor decision was made and applied (restore `1.27.1`), v0.5.0 was released through the full runbook with zero skipped steps (the new `scripts/pre-release-check.sh` gated it), all six in-house consumers were swept to v0.5.0 with their version surfaces (flake inputs, vendorHashes, CI/nix Go pins) realigned, the LSP was verified healthy, and the HEAD-freshening test debt was closed with three new spec/unit pins and two filed benchmark pairs. Client statement coverage rose 99.1% → 99.7%; `persistFreshened` and `cloneHeader` sit at 100%.

## b. Work items

| #  | Item                                                                                                    | Verdict                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| -- | ------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1  | TODO#2 Go-floor decision (M1/F3)                                                                        | **DONE** — restored `go 1.27.1` (commit `45e68c8`): the published v0.4.0 contract, README badge, and all three CI pins say 1.27.1; the daemon's unreviewed `1.27` relaxation (969d077) was drift, not a decision. Full race suite green on the restored directive. The daemon has NOT re-relaxed it since.                                                                                                                                                                                                             |
| 2  | TODO#4 LSP fix (M4/F22–F23)                                                                             | **DONE (already fixed upstream)** — the deployed `~/.config/crush/crushrc` launches gopls and golangci-lint-ls with `--env GOTOOLCHAIN auto` (crush-config repo, deployed 2026-09-22). Verified live: both LSPs `ready`, zero project errors. AGENTS.md's stale "dead diagnostics" note replaced with the accurate story.                                                                                                                                                                                             |
| 3  | TODO#5 HEAD marker spec pin (M5/F24–F26)                                                                | **DONE** — `TestSpecHeadFresheningDoesNotPersistFromCacheMarker` (commit `2ac636b`): primes the store with a marker-carrying entry (legacy/drifted state), runs a confirming HEAD, asserts the stored entry comes back clean, the freshening ran (Date updated), and the rebuilt GET still wears the marker — proving active stripping, not vacuous pass.                                                                                                                                                             |
| 4  | TODO#6 cloneHeader prove-or-delete + coverage (M6/F27–F29)                                              | **DONE — proved + kept** — the nil branch is unreachable through every current production path (store nil-guards its Clone; persistFreshened clones headers that already passed through cloneHeader/freshenedHeader); the proof lives in cloneHeader's doc comment, `TestCloneHeaderNilReturnsEmpty` pins the guard's contract, and `TestFreshenFromHeadKeepsValidatorWhenHeaderLacksETag` pins the last uncovered statement (the validator fallback). Commit `8c1c1a7` (+ daemon `bf21b4b` for the doc comment). |
| 5  | Plan M7a HEAD-path bench pair (F30–F31)                                                                 | **DONE** — `BenchmarkTransportHeadFreshening` added (exported-surface only, compiles on both arms); before-arm captured in a `4ba1ed8^` worktree, after-arm interleaved on master. Measured cost of the 06-18 freshening dedup: **+432 B/op, +3 allocs/op (12 → 15)** per freshening HEAD. Filed `reports/bench/2026-09-23_{before,after}-head-freshening.txt`.                                                                                                                                                  |
| 6  | TODO#7 stored-validator before-state (F32)                                                              | **DONE** — full-suite bench (`-benchmem -count=6 ./...`) captured from a `07fe65c^` worktree, inventory-identical to the 09-18 after baseline (9 benches × 6 counts). Filed `reports/bench/2026-09-23_before-typed-code-stored-validator.txt`; report 2026-09-18_20-44 b1/f.7 annotated closed.                                                                                                                                                                                                                     |
| 7  | Plan M8 pre-release-check.sh (F33–F35)                                                                  | **DONE** — `scripts/pre-release-check.sh` (landed in daemon commit `af3c6e5`): build/vet/race/lint/fmt-diff/erraudit (with `[feature:logger]` filter)/nolint-audit/`go mod verify`/replace + pseudo-version bans/go-directive ↔ CI-pin parity. Dry-run exit 0 on master; parity and pseudo-version failure branches verified standalone. Wired into AGENTS.md Release Conventions + CONTRIBUTING.                                                                                                                  |
| 8  | Plan M9 GoDoc examples (F36–F40)                                                                        | **DONE** — commit `32c9793`: `ExampleParseETag` (round-trip incl. weak form + refused unquoted), `ExampleETag_weakVsStrong`, `ExampleParseETagList` (wildcard excluded), `ExampleFreshenPerRFC`, `ExampleFreshenNone`. All `// Output:`-tested; rendered on pkg.go.dev at the tag.                                                                                                                                                                                                                               |
| 9  | TODO#1 Release v0.5.0 (M2/F4–F14)                                                                       | **DONE** — CHANGELOG cut (`af2b6d1`) → master pushed → **CI green on the exact commit** (run 35803874554) → annotated tag `v0.5.0` on `af2b6d1` → pushed → frozen tag run green (35804030500) → proxy `.info` hash == `af2b6d1` → sum.golang.org recorded (`h1:WivCc…`) → clean-room `go get …/metrics@v0.5.0` + Attach/HitRatio smoke (go.mod floor 1.27.1 confirmed on the wire) → GitHub Release v0.5.0 Latest non-prerelease → pkg.go.dev renders metrics + examples within the hour. |
| 10 | TODO#3 Consumer sweep (M3/F15–F21, go-ecosystem-upgrade skill)                                          | **DONE for all six real consumers** — see §c. Enumerated with `--no-ignore-vcs --hidden`: ~100 direct requires across ~40 repos, but only httputil, go-github-kit, library-policy, nsfw-classifier, DiscordSync, cqrs-htmx (22 modules) actually import go-etag in source or tests; the rest are dead requires (documented, untouched — each repo's own hygiene). Baseline build+test recorded before every touch (F11).                                                                                          |

## c. Consumer sweep detail (TODO#3)

| Repo              | Before → After              | Version surfaces realigned                                                                                                                                                                                              | Gates verified                                                                                                                                                                                                  |
| ----------------- | --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| httputil          | v0.3.1 → v0.5.0             | go.work directive 1.27 → 1.27.1 (`go work edit`); CI + release `GOTOOLCHAIN` pins go1.26.7 → go1.27.1; devshell `go_1_26` → `go_1_27` (nixpkgs resolves exactly 1.27.1)                                                | build+test both workspace modes green; errorTemplate-parity suite green (F51 verified as a side effect); devshell builds+vets; no project FOD (benchstat's vendorHash unaffected)                                |
| go-github-kit     | v0.3.1 → v0.5.0             | devshell `go_1_26` → `go_1_27`; go-error-family rides 0.10.0 → 0.10.1; CI uses go-version-file (self-following)                                                                                                          | already on the FreshenOn304 API (no code migration); build+test green; flake evals; lint loads under the new toolchain (pre-existing varnamelen/wrapcheck findings in untouched files documented)                 |
| nsfw-classifier   | v0.3.1 → v0.5.0             | vendor/ refreshed; project FOD vendorHash rotated (daemon raced the first rotation; re-applied deterministically in `478c84c`)                                                                                          | build+test green; `nix build` completed from the tagged commit                                                                                                                                                    |
| library-policy    | v0.3.1 → v0.5.0             | flake input go-etag repinned `ref=master` lock ede91ab → **af2b6d1 (the v0.5.0 tag commit)**; vendorHash rotated; repo-local check derivations overridden to the goPkgAttr toolchain; `enableGoimports = false` (sandbox toolchain-download class); GOEXPERIMENT trimmed to `jsonv2,simd` (1.27 rejects `goroutineleakprofile`); self-scan `.err` trailing whitespace trimmed | devshell build+test green; `nix build` green; `nix flake check` green except one pre-existing finding (below)                                                                                                  |
| DiscordSync       | v0.4.0 → v0.5.0             | flake input `rev=16369cc…` (v0.4.0 commit) → `rev=af2b6d1…` (v0.5.0 commit); vendorHash rotated                                                                                                                          | drift-guard suite green (34s); build+test green except 2 failures proven pre-existing/unrelated (mime test fails identically at pre-bump commit; disk-space health check trips on /tmp at 16.5% free); `nix build` green |
| cqrs-htmx (22 modules) | v0.4.0 → v0.5.0        | none beyond go.mod/go.sum (already 1.27.1 floor; poisoned-tag verify-tag fixture keeps its deliberate v0.2.0 pin)                                                                                                       | preflight-tree-check OK; per-module GOWORK=off get+tidy+build+vet all green; workspace build green; `check-modules` fully green after re-running the canonical CSS builders (a hook had rewritten 2 bundles without --minify — fixed with the gate's own commands)             |

**Sweep pre-existing findings (each consumer repo's own backlog, handed off via TODO_LIST #2):** library-policy `structured_formatters.go` at 307/300 lines (since 09-13; a domain-code split, out of a dep sweep's scope per F10); cqrs-htmx `check-go-toolchain.sh` red (flake toolchain 1.26.7 < 1.27.1 floor — predates the sweep; floor came from its own deps); DiscordSync mime + disk tests; ~35 fleet repos carrying dead go-etag requires; go-github-kit's pre-existing lint findings.

## d. Self-review (honest)

1. **The daemon raced me four times** (transport.go doc comment, bench_test.go, pre-release script, DiscordSync/nsfw/classifier sweeps) — every time the content landed intact in a `chore: auto-commit` and I verified rather than re-committed. The nsfw-classifier vendorHash was lost once between buildflow's fix and my check (root cause unexplained); re-applied deterministically from the recorded `got:` hash and rebuilt from the commit to confirm.
2. **The sweep's ecosystem reality outgrew the plan's "six consumers"** — ~40 repos pin go-etag, but import triage shrank the real set to exactly the plan's six. The dead-require fleet (~35 repos) is documented rather than cleaned: tidy would REMOVE the requires (a policy decision per repo), not bump them.
3. **library-policy needed four infra fixes beyond the bump** (check-derivation toolchain, whitespace, goimports sandbox, GOEXPERIMENT) — all pre-existing reds surfaced by running `nix flake check` as the gate; fixed because the gate could not be green without them, each with a comment at the fix site. The one production-code finding (307 lines) was deliberately left with justification in the commit.
4. **Baseline sloppiness caught mid-flight (F11):** DiscordSync's baseline test filter let INFO logs hide FAIL lines; the pre-bump worktree re-run proved the failures pre-existing. Lesson re-learned: filter for failures, not against noise.
5. **What I did NOT do:** ROADMAP OQ2–OQ8 (owner decisions, not TODO_LIST items — promoted to TODO_LIST #1 for the next session); pushing the six consumer repos (not authorized; listed in §e); M10–M27 plan tiers (explicitly out of "the whole TODO list" scope; promoted to TODO_LIST #3).

## e. Bounded leftovers

1. Six consumer repos hold unpushed sweep commits (httputil +2, go-github-kit +1, nsfw-classifier +2, library-policy +4, DiscordSync +1, cqrs-htmx sweep+css commits) — push on owner go.
2. ROADMAP OQ2–OQ8 decision batch — TODO_LIST #1, gates the roadmap epics.
3. Plan M10–M27 (benchstat, erraudit-CI, coverage floor, fuzz expansion, README sections, Theme 1/3 docs, v1.0.0 criteria) — TODO_LIST #3.
4. The pre-existing consumer findings in §c — handed to each repo.

## f. Gate evidence

- go-etag: `scripts/pre-release-check.sh` exit 0 on `af2b6d1` (build/vet/race/lint/fmt/erraudit/nolint-audit/mod-verify/replaces/pseudo/parity all green); CI green on exact commit and on the tag (frozen run).
- Proxy: `https://proxy.golang.org/…/v0.5.0.info` → Hash `af2b6d1d02f94f2ae6ab79374c0428e519666173`, Ref `refs/tags/v0.5.0`. sum.golang.org lookup recorded.
- Clean-room: fresh module, `go get github.com/larsartmann/go-etag/metrics@v0.5.0`, Attach/HitRatio smoke ran (`generated=2 … ratio=0.00`), `go list -m` confirms v0.5.0.
- Coverage after hardening: root 100.0 / server 98.6 / **client 99.7** / entitytag 98.9 / metrics 96.8 (FEATURES stamp updated).
- Consumers: per-repo gates as tabled in §c.

## g. Questions for the owner

1. **Push the six consumer repos?** All sweep commits are verified and local; one `git push` each (cqrs-htmx's pre-push release-train hook will run strict — its toolchain-check pre-existing red does not gate pushes).
2. **Fleet hygiene for dead go-etag requires (~35 repos at v0.3.1, never imported):** a one-shot tidy sweep (removes the requires), or leave each repo to its own devices?
3. **The v0.5-cycle's owner decisions (OQ2–OQ8)** still await the structured prompt (TODO_LIST #1) — say the word and it ships.
